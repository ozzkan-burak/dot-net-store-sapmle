@model IEnumerable<Category>
<div class="list-group my-2">
    @foreach (var category in Model)
    {
        <a class="list-group-item list-group-item-action" asp-action="Index" asp-controller="Product" asp-route-categoryId="@category.CategoryId">
            @category.CategoryName
        </a>
    }
</div>